'use strict';
angular
	.module('huguesApp')
	.controller('OnsiteValidationCtrl', function($state, ngNotify, $location, onsiteFactory) {
		function initialData() {
			
		}

        var vm = this;
        initialData();
	});
